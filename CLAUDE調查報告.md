# NERL 控制器強化學習設計調查報告

## 一、目前的設計分析

### 1.1 狀態空間設計（17維）

目前的狀態空間包含以下特徵：

#### 當前路口狀態（8維）
1. **方向編碼** (dir_code/2.0)：當前允許方向，歸一化到[0,1]
2. **時間特徵** (time_since_change)：自上次信號變化以來的時間
3. **機器人數量** (h_count, v_count)：水平和垂直方向的機器人數
4. **優先級比例** (h_priority/h_count, v_priority/v_count)：送貨機器人的比例
5. **等待時間** (h_wait_time, v_wait_time)：各方向平均等待時間

#### 相鄰路口狀態（8維）
6. **鄰居數量** (neighbors['count']/4.0)：相鄰路口數量（最多4個）
7. **鄰居機器人總數** (neighbor_robots)
8. **鄰居優先級機器人數** (neighbor_priority)
9. **鄰居優先級比例** (total_priority/total_robots)
10. **鄰居平均等待時間** (neighbor_wait)
11. **方向分佈** (水平/垂直鄰居比例)
12. **負載均衡指標**：鄰居之間的負載差異

#### 全局特徵（1維）
13. **檢貨台排隊長度** (picking_queue/MAX_CAPACITY)：V5.0新增

### 1.2 動作空間設計（6個動作）

V7.0版本的動作空間包含：
- **動作 0**：保持當前方向
- **動作 1**：切換到垂直方向  
- **動作 2**：切換到水平方向
- **動作 3**：30%限速（V7.0新增）
- **動作 4**：50%限速（V7.0新增）
- **動作 5**：取消限速（V7.0新增）

### 1.3 獎勵系統設計

目前支援兩種獎勵模式：

#### Step獎勵模式（V7版本）
- **通過獎勵**：len(passed_robots) * 0.5
- **等待懲罰**：根據等待時間累積，超過100 ticks加重懲罰
- **穩定性獎勵**：
  - max_wait < 200: +0.2
  - max_wait > 500: -1.0
- **限速動作獎勵**：使用限速動作時 +0.1
- **關鍵路口權重**：關鍵路口的獎勵乘以 critical_weight（預設2.0）

#### Global獎勵模式
- 每步返回0，只在回合結束時計算全局獎勵
- 考慮整體訂單完成率、平均等待時間等系統級指標

### 1.4 神經網路架構

使用 EvolvableNetwork：
- 輸入層：17維狀態
- 隱藏層1：128個神經元 + BatchNorm + ReLU + Dropout(0.3)
- 隱藏層2：64個神經元 + BatchNorm + ReLU + Dropout(0.3)
- 輸出層：6個動作

### 1.5 進化算法參數
- 族群大小：20
- 菁英保留：20%（4個個體）
- 錦標賽大小：4
- 交叉率：0.8
- 突變率：0.2
- 突變強度：0.15
- 進化間隔：1000步

## 二、發現的問題與改進建議

### 2.1 獎勵系統複雜度問題

**現狀問題**：
1. unified_reward_system.py 有591行，包含多個版本（V3/V6/V7）的混合邏輯
2. Step和Global模式的設計理念不統一，難以比較
3. 獎勵函數包含太多手動調整的權重和閾值

**改進建議**：
1. **簡化獎勵設計**：統一使用一種模式，建議使用改進的Step模式
2. **減少手動參數**：使用更少但更有意義的獎勵項
3. **模組化重構**：將不同獎勵組件分離，便於實驗和調整

### 2.2 狀態空間設計問題

**現狀問題**：
1. 17維特徵可能存在冗餘（如同時包含數量和比例）
2. 部分特徵的重要性不明確
3. 缺乏對交通流模式的直接編碼

**改進建議**：
1. **特徵工程**：
   - 移除冗餘特徵（如絕對數量vs比例）
   - 增加時序特徵（如最近N步的流量變化）
   - 考慮加入路口的空間位置編碼
2. **降維考慮**：使用主成分分析識別關鍵特徵

### 2.3 動作空間設計問題

**現狀問題**：
1. 6個動作中，限速動作（3,4,5）的使用率可能偏低
2. 動作效果不夠直觀，限速百分比是否合理？
3. 缺乏對動作效果的即時反饋

**改進建議**：
1. **簡化動作空間**：
   - 考慮將6個動作減少到4個：保持、切換水平、切換垂直、智能限速
   - 智能限速可以根據擁堵程度自動調整
2. **動作設計原則**：
   - 每個動作應該有明確且可預測的效果
   - 動作之間應該有明顯的區別

### 2.4 學習穩定性問題

**現狀問題**：
1. NERL的進化過程可能不穩定，容易陷入局部最優
2. 缺乏對探索與利用的平衡機制
3. 評估時間（evolution_interval=1000）可能不足

**改進建議**：
1. **改進探索機制**：
   - 在早期世代增加更多隨機性
   - 使用多樣性獎勵防止過早收斂
2. **延長評估時間**：至少2000-3000步才能看到策略效果
3. **引入漸進式學習**：從簡單場景逐步增加複雜度

## 三、重新設計方案（初步建議）

### 3.1 統一的獎勵函數設計

```python
def calculate_reward(self, intersection, action, tick):
    """簡化但有效的獎勵函數"""
    
    # 1. 效率獎勵：通過的機器人數量
    throughput = len(passed_robots)
    efficiency_reward = throughput * 0.5
    
    # 2. 公平性懲罰：防止某個方向等待過久
    max_wait = max(h_max_wait, v_max_wait)
    fairness_penalty = -0.1 * (max_wait / 100) ** 2  # 二次懲罰
    
    # 3. 穩定性獎勵：鼓勵平滑的交通流
    if action == 0:  # 保持當前方向
        stability_bonus = 0.1
    else:
        stability_bonus = -0.05  # 輕微懲罰頻繁切換
    
    # 4. 能源效率：根據實際速度調整
    if speed_limit_active:
        energy_bonus = 0.2 * (1 - speed_limit_ratio)
    else:
        energy_bonus = 0
    
    total_reward = efficiency_reward + fairness_penalty + stability_bonus + energy_bonus
    return np.clip(total_reward, -1, 1)
```

### 3.2 精簡的狀態空間（建議10-12維）

1. **核心交通狀態**（4維）：
   - 水平/垂直方向等待機器人數
   - 水平/垂直方向最大等待時間

2. **優先級信息**（2維）：
   - 水平/垂直方向高優先級機器人比例

3. **時序特徵**（2維）：
   - 當前方向持續時間
   - 最近切換頻率

4. **鄰域信息**（2維）：
   - 上下游擁堵程度
   - 鄰居平均負載

5. **全局狀態**（2維）：
   - 系統總體擁堵水平
   - 當前時段（高峰/低谷）

### 3.3 簡化的動作空間（4個動作）

1. **保持當前方向**：適合流量穩定時
2. **切換到水平**：明確的方向選擇
3. **切換到垂直**：明確的方向選擇  
4. **自適應限速**：根據擁堵程度自動調整速度（0.3-1.0）

### 3.4 改進的訓練策略

1. **漸進式難度**：
   - 初期：20個機器人，學習基本控制
   - 中期：30個機器人，學習防擁堵
   - 後期：35-40個機器人，學習極限情況

2. **多目標平衡**：
   - 主目標：訂單完成率 > 95%
   - 次目標：最大等待時間 < 300
   - 約束：能源消耗最小化

3. **評估指標統一**：
   - 使用相同的評估環境和指標
   - 確保Step獎勵的累積與最終效果一致

## 四、實施優先級

1. **第一階段**：簡化獎勵函數，統一為單一模式
2. **第二階段**：優化狀態空間，移除冗餘特徵
3. **第三階段**：調整動作空間，提高可解釋性
4. **第四階段**：改進訓練流程，增加穩定性

## 五、預期效果

通過上述改進，預期達到：
1. **學習效率提升**：更快收斂到有效策略
2. **策略可解釋性**：動作選擇更加合理
3. **性能穩定性**：減少訓練結果的方差
4. **實際應用價值**：更容易遷移到真實場景

## 結論

目前的NERL實現功能完整但過於複雜。建議通過簡化設計、統一模式、優化特徵等方式，提升系統的學習效率和實用性。特別是要解決獎勵函數過於複雜和狀態空間冗餘的問題，這將有助於提升論文的理論貢獻和實踐價值。