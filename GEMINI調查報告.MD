# NERL 控制器設計調查報告

## 1. 現有設計分析

根據對 `ai/controllers/nerl_controller.py` 和 `ai/unified_reward_system.py` 的分析，當前的 NERL 控制器設計如下。

### 1.1. 狀態空間 (State Space)

狀態空間設計得相當全面，試圖捕捉交叉口的局部和鄰近資訊。每個交叉口的狀態是一個 **17 維的向量**，經過自適應正規化 (`TrafficStateNormalizer`) 處理。

其維度構成如下：

- **當前交叉口資訊 (8 維):**
    1.  `dir_code`: 當前允許方向 (0: 無, 1: 垂直, 2: 水平)，正規化至 [0, 1]。
    2.  `time_since_change`: 自上次號誌變換至今的時間。
    3.  `h_count`: 水平方向機器人數量。
    4.  `v_count`: 垂直方向機器人數量。
    5.  `h_priority`: 水平方向高優先級 (送貨中) 機器人比例。
    6.  `v_priority`: 垂直方向高優先級 (送貨中) 機器人比例。
    7.  `h_wait_time`: 水平方向平均等待時間。
    8.  `v_wait_time`: 垂直方向平均等待時間。

- **相鄰交叉口資訊 (8 維):**
    9.  `neighbors['count']`: 相鄰交叉口數量 (正規化)。
    10. `neighbor_robots`: 相鄰交叉口總機器人數量。
    11. `neighbor_priority`: 相鄰交叉口高優先級機器人總數。
    12. `neighbor_priority_ratio`: 相鄰交叉口高優先級機器人比例。
    13. `neighbor_wait`: 相鄰交叉口平均等待時間。
    14. `neighbor_h_ratio`: 相鄰交叉口中，水平通行方向的比例。
    15. `neighbor_v_ratio`: 相鄰交叉口中，垂直通行方向的比例。
    16. `neighbor_load_balance`: 相鄰交叉口負載均衡指標 (最大負載 - 最小負載)。

- **全局資訊 (1 維):**
    17. `picking_queue`: 揀貨台的排隊長度。

**分析:**
- 狀態表示非常豐富，但也可能因此引入過多雜訊，使得學習變得困難。
- `TrafficStateNormalizer` 的使用是個好的實踐，可以幫助神經網路更好地處理不同範圍的輸入。

### 1.2. 動作空間 (Action Space)

動作空間是離散的，共有 **6 個動作**。

1.  `action 0`: **保持 (Keep)** 當前方向。
2.  `action 1`: 切換至 **垂直 (Vertical)** 方向。
3.  `action 2`: 切換至 **水平 (Horizontal)** 方向。
4.  `action 3`: **限速 30%** (不改變方向)。
5.  `action 4`: **限速 50%** (不改變方向)。
6.  `action 5`: **取消限速** (不改變方向)。

**分析:**
- 包含了方向控制和速度控制，這對於平衡吞吐量和能源/穩定性至關重要。
- `nerl_controller.py` 中存在硬編碼的防鎖死邏輯，它會在 AI 決策之前先生效，這是一種安全機制，但也可能影響 AI 的學習探索。

### 1.3. 獎勵系統 (Reward System)

獎勵系統是整個設計中最複雜、演化版本最多的部分，由 `UnifiedRewardSystem` 類別管理。它明確支援兩種模式：`global` 和 `step`。

#### **`reward_mode = "global"`**

- **核心思想**: 在一次完整的模擬 (episode) 結束後，根據最終的宏觀指標計算一個單一的獎勵值。
- **V5.1 公式**:
  `R_global = (完成訂單數 * 完成獎勵) / (能源成本 + 時間成本 + 溢出懲罰) + 無溢出獎勵`
- **優點**: 直接優化最終的商業目標 (訂單、能耗)。
- **缺點**: 獎勵非常稀疏 (Sparse Reward)，信號延遲巨大，使得 AI 極難學習到每一步動作與最終結果之間的因果關係。

#### **`reward_mode = "step"`**

- **核心思想**: 在每個交叉口的每一次決策後，立即給予一個即時獎勵。
- **存在多個版本**:
    - `calculate_step_reward`: 一個較早的、基於優先級的簡單版本。
    - `calculate_step_reward_v7`: **當前實際使用的版本**。這是一個非常複雜的函數，試圖將多種目標融合在一起。
- **`v7` 版本的獎勵構成**:
    1.  **流通獎勵**: 機器人通過的基礎獎勵，**關鍵路口權重更高**。
    2.  **能源獎勵**: 對低速、低能耗通過的機器人給予額外獎勵。
    3.  **訂單完成獎勵**: 對於正在運送貨物的機器人通過給予額外獎勵。
    4.  **等待懲罰**: 對等待的機器人進行懲罰，非關鍵路口懲罰較輕。
    5.  **切換懲罰**: 變換號誌的懲罰，關鍵路口懲罰較輕。
    6.  **擁堵管理獎勵**: 當揀貨站擁堵時，獎勵讓低優先級機器人等待的行為。
    7.  **限速控制獎勵**: 當揀貨站擁堵時，獎勵使用限速的行為。
    8.  **信號放大**: 最終獎勵值會乘以一個放大係數 (例如 `* 5`)。

**分析:**
- **`global` vs `step`**: 這兩種模式的設計哲學完全不同。`global` 模式目標明確但難以學習，`step` 模式學習信號密集但難以設計，容易導致 AI "鑽漏洞" (Reward Hacking)，只優化短期指標而忽略長期目標。
- **激進程度 `a` 和 `b`**: 在程式碼中沒有找到明確的 `a` 和 `b` 參數。我推測這可能是指不同版本的獎勵函數或權重配置，例如 `v7` 獎勵函數中對**關鍵路口** (`critical_intersections`) 的不同權重 (`critical_weight`)，或者不同獎勵項之間的權重比例，這些都直接影響了控制器的 "激進" 程度。例如，高流通獎勵、低等待懲罰會讓控制器更激進地追求吞吐量。

---

## 2. 初步重構建議

基於您的重構目標 (統一設計、專注穩定性與能效) 和我對現有系統的分析，在您提供參考文獻之前，我提出以下初步建議。

### 2.1. 統一獎勵模式：融合 `global` 與 `step`

與其二選一，不如設計一個混合式 (Hybrid) 獎勵函數，將兩者的優點結合。

**建議作法**:
- **主要使用 `step` 獎勵**: 為 AI 提供密集的學習信號。
- **引入 `global` 目標作為 `step` 獎勵的調節因子**: 將宏觀指標 (如揀貨台排隊長度、系統總能耗) 作為 `step` 獎勵函數中的一個變數。

**範例**:
`R_step_hybrid = R_flow - C_wait * (1 + f(Q_picking)) - C_switch`

- `R_flow`: 基礎的流通獎勵。
- `C_wait`: 基礎的等待懲罰。
- `Q_picking`: 當前揀貨台的排隊長度。
- `f(Q_picking)`: 一個遞增函數。當全局排隊長度 `Q_picking` 增加時，等待懲罰 `C_wait` 的權重也隨之動態增加。

**優點**:
- **統一性**: 只有一種獎勵模式需要維護和調整。
- **動態調節**: AI 在系統空閒時可以表現得 "激進" (追求吞吐量)，在系統擁堵時會自動變得 "保守" (更注重避免等待，從而提升穩定性)。
- **因果更直接**: 將全局狀態直接反映到局部決策的獎勵中。

### 2.2. 簡化並統一 "激進程度"

當前的 `v7` 獎勵函數試圖一次性解決太多問題，導致權重和邏輯複雜，難以分析和調優。

**建議作法**:
1.  **簡化獎勵維度**: 將 `step` 獎勵的核心目標簡化為 2-3 個，並使其正交化 (Orthogonal)。
    - **目標1: 提升流通效率 (Throughput)**: `+1` 分給每個通過的機器人。
    - **目標2: 降低等待時間 (Stability)**: `-0.1` 分給每個在路口等待的機器人 (每 tick)。
    - **目標3: 節約能源 (Energy)**: `-0.01` 分乘以該 tick 內所有機器人的總能耗。

2.  **統一激進程度**: 將 "激進程度" 抽象為一個單一、可解釋的超參數 `alpha`。
    `R_final = alpha * R_throughput + (1 - alpha) * (R_stability + R_energy)`

    - `alpha` 接近 1.0: 控制器更 "激進"，專注於吞吐量。
    - `alpha` 接近 0.0: 控制器更 "保守"，專注於穩定性和節能。

**優點**:
- **可解釋性**: 獎勵函數的目標變得非常清晰。
- **實驗方便**: 您可以輕易地透過調整 `alpha` 這一個參數來進行一系列關於控制器在 "性能-穩定性" 光譜上不同點的實驗，這對於論文寫作和圖表生成非常有幫助。
- **移除隱藏複雜性**: 廢除 "關鍵路口" 等硬編碼的特殊邏輯，讓 AI 自己去學習哪些路口更重要。如果某些路口真的重要，AI 自然會在那裡學到不同的策略。

### 2.3. 狀態與動作空間的微調

- **狀態空間**:
  - **建議**: 可以考慮暫時移除一些高度相關或可能引入雜訊的維度，例如相鄰路口的 `neighbor_load_balance` 和方向比例 (`neighbor_h_ratio`, `neighbor_v_ratio`)，先用一個更簡潔的狀態空間進行訓練，如果效果不佳再逐步加回。

- **動作空間**:
  - **建議**: 目前的 6 個動作是合理的。特別是限速動作，是實現穩定性和節能的關鍵。建議保留。可以考慮將防鎖死邏輯從 "強制執行" 改為給予一個巨大的負獎勵，讓 AI 自己學會避免鎖死，但這會增加學習難度，初期可以保持現狀。

### 總結
我的核心建議是：
1.  **統一獎勵模式**：採用一個受全局指標動態調節的、混合式的 `step` 獎勵。
2.  **簡化獎勵函數**：將多個複雜的獎勵項簡化為幾個核心、正交的目標。
3.  **統一激進程度**：引入單一超參數 `alpha` 來控制 "性能 vs 穩定/節能" 的權衡，方便實驗和分析。

這將使您的 RL 系統更簡潔、更易於理解和控制，也更容易產生清晰、有說服力的實驗結果來支撐您的論文論點。
