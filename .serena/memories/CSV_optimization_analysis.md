# CSV 優化分析

## 現有實現
- 使用 `write_to_csv()` 函數，每次調用都會打開文件並寫入一行
- 訂單完成時立即寫入 `order-finished.csv`
- 進程隔離：使用 `process_id` 為每個進程創建獨立文件

## 可能的性能問題
1. 每個 tick 都可能有 I/O 操作
2. 檔案開關頻繁（mode='a' 追加模式）
3. 並行訓練時的文件競爭

## 優化建議
- 批量寫入：累積數據到記憶體，定期或結束時寫入
- 使用 DataFrame 批量處理
- 異步 I/O 或獨立的寫入線程