# 開發計畫

## 已完成項目
- [x] NERL 訓練分析器重寫與選單整合：
  - 重寫 `test/nerl_training_analyzer.py` 直接解析 `genXXX/fitness_scores.json`，輸出圖表、統計與報告。
  - `test/experiment_menu.py` 第 11 項改為呼叫新版分析器，輸入 `models/training_runs/<run>` 即可分析。
- [x] 基準模型圖表生成相容性：擴充 `test/experiment_menu.py` 支援 `queue_based_*`、`time_based_*`、`baseline_*` 與子資料夾 `time_based/`、`queue_based/`、以及舊格式 `tb_*`、`qb_*`，並修正結果根目錄為 `test/results`。
- [x] 圖表中文顯示修正：`data_cleaning_comparison.png` 摘要文字改用全域字型（移除 monospace），避免中文變方塊。
- [x] Baseline 分析強化：
  - 新增清洗流程（全域生成異常、群組內 0.85×trimmed-mean、跨參數一般異常），並產生清洗統計
  - 兼容舊資料單位（利用率>1 乘 0.15）
  - 補齊欄位（完成數/總單數/每單能耗），參數圖加入 n 標註，圖表使用清洗後資料

- [x] 選單自訂功能強化：
  - Time-Based：支援自訂「時間配比」清單（A:B 且 A+B=100），預設清單亦可沿用
  - Time-Based / Queue-Based：均支援自訂「機器人數量」清單（預設 `[25, 30]` 或可自行輸入）



- [x] 調查：新增 `GPT調查報告.md`（僅文檔）彙整 NERL 現況與統一建議；不修改任何程式碼。

- [x] 最終比較實驗（三控制器）
  - `test/experiment_menu.py` 新增「最終比較實驗（三控制器）」選單，可一鍵執行 NERL Global（`models/final_models/nerl_global_best.pth`）、Time-Based 與無控制。
  - 支援自訂評估時長、每控制器重複次數、是否並行與最大並行數；每次運行使用獨立 `SIMULATION_ID` 與 `NETLOGO_STATE_DIR/FILE`、`ASSIGN_ORDER_CSV`，並啟用 `USE_EXISTING_ORDERS=1`，避免 I/O 衝突。
  - `evaluate.py` 新增以環境變數控制的時間序列輸出（`ENABLE_TIME_SERIES`、`TS_INTERVAL`），輸出於 `test/final_exp_results/<controller>/<SIMULATION_ID>/time_series_*.csv`。

## 進行中 / 新增項目
- [ ] NERL 重構（GLOBAL 適應度 + 穩定性、資料驅動權重）
  - [x] 產出方案文件：`NERL控制器重構方案.md`
  - [ ] 備份現行：複製 `ai/controllers/nerl_controller.py`、`ai/unified_reward_system.py` 為 legacy 版
  - [ ] Stop-and-Go 蒐集：在機器人狀態切換（停→走）時遞增 `warehouse.stop_and_go`；在 `evaluate.py` 寫入 `total_stop_go`
  - [ ] Fitness 配置化：新增 `ai/config/nerl_fitness_config.json`，載入權重/門檻
  - [ ] 校準腳本：由清洗後資料自動估 `O_ref/E_ref/U_ref/SG_thr/W_thr`
  - [ ] 小樣本驗證：25/30 各 N=5；消融（去除 λ_sg 或 λ_wait）
  - [ ] 最終對比：25/30 各 N=20（100k ticks）
  - [x] 修正（NERL）：`picking_queue` 正規化與 `action_to_direction()` 對齊系統語意
  - [x] 等待時間：改為「事件累積」指標與獎勵（ai/unified_reward_system.py、evaluate.py），避免快照偏誤
  - [x] 調整權重：將 GLOBAL fitness 的 `w_energy` 降至 `0.00333`（公平性考量）
  - [x] 設定一致：固定 NERL 訓練 `ROBOT_COUNT=25`（`experiment_menu.py` 第 9 項與 `train.py`）
