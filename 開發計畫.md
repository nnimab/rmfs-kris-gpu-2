# 開發計畫

## 已完成項目
- [x] 基準模型圖表生成相容性：擴充 `test/experiment_menu.py` 支援 `queue_based_*`、`time_based_*`、`baseline_*` 與子資料夾 `time_based/`、`queue_based/`、以及舊格式 `tb_*`、`qb_*`，並修正結果根目錄為 `test/results`。
- [x] 圖表中文顯示修正：`data_cleaning_comparison.png` 摘要文字改用全域字型（移除 monospace），避免中文變方塊。
- [x] Baseline 分析強化：
  - 新增清洗流程（全域生成異常、群組內 0.85×trimmed-mean、跨參數一般異常），並產生清洗統計
  - 兼容舊資料單位（利用率>1 乘 0.15）
  - 補齊欄位（完成數/總單數/每單能耗），參數圖加入 n 標註，圖表使用清洗後資料

